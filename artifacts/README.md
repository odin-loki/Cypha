# artifacts/

Local-only captures (`cypha_bench_full_baseline.ps1`, tune grids, timing dumps). Gitignored except this file.

Historical committed traces live in [`data/archive/profiles/`](../data/archive/profiles/).
