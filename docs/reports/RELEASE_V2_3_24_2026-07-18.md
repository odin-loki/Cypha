# Release v2.3.24 — 2026-07-18

| Step | Result |
|------|--------|
| `git push origin main` | OK |
| Tag `v2.3.24` | On remote at HEAD after continuum |
| GitHub Release | https://github.com/odin-loki/Cypha/releases/tag/v2.3.24 |
| Windows asset | https://github.com/odin-loki/Cypha/releases/download/v2.3.24/cypha-2.3.24-windows-x86_64.zip (**MSVC**, ~7.5 MB from Release job `windows-x86_64`) |
| Linux CLI / AppImage | Tarball builds; AppImage previously failed on missing `packaging/cypha.png` — icon added; AppImage step is non-blocking so CLI still ships |
| Windows CI gate | `windows_msvc` green on `3d28d7f` |
| `gh auth login` | Still incomplete for CLI (`read:org` missing on stored PAT); publish used git-credential + REST API |

## Packaging notes

- Windows: native MSVC via `scripts/package_release_windows.ps1` (MinGW retired).
- AppImage: requires `packaging/cypha.png` (`Icon=cypha`).

Notes generated via `scripts/publish_release.ps1 -DryRun`.
