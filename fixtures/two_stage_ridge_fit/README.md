# Two-stage ridge fit parity

**`two_stage_dif_ridge_fit_from_llr`** vs sidecar (LLR matrix frozen in JSON).

**Frozen fixture.** Gate: **`ctest -R native_regression_two_stage_ridge_fit`**. Override: **`CYPHA_TWO_STAGE_RIDGE_FIT_PARITY_BIN`**.
