# Release v2.3.24 — 2026-07-18

| Step | Result |
|------|--------|
| `git push origin main` | OK |
| Tag `v2.3.24` | On remote at HEAD after continuum |
| GitHub Release | https://github.com/odin-loki/Cypha/releases/tag/v2.3.24 |
| Windows asset | https://github.com/odin-loki/Cypha/releases/download/v2.3.24/cypha-2.3.24-windows-x86_64.zip (~21 MB; packaged from local `native/build_math`) |
| Linux / AppImage CI | Prior Release workflow failed at package step (see fix below); re-tag after packaging fix to refresh CI assets |
| `gh auth login` | Still incomplete for CLI (`read:org` missing on stored PAT); publish used git-credential + REST API |

## Packaging CI root cause

`scripts/package_release_{windows,linux}.sh` wrote README via unquoted `<<EOF` with backticks around `cypha_qt_shell` / `native/`. Under `set -e`, command substitution aborted the package step on Ubuntu runners. Fixed: plain text, no backticks; UTF-8 BOM stripped.

Notes generated via `scripts/publish_release.ps1 -DryRun`.
