# Cypha development plan — 2026-07-11

**Last updated:** 2026-07-11
**Author:** agent session (research via Composer 2.5 subagents [`f4066b99`](f4066b99-7f0b-490c-ac3c-b7934b8411b1), [`96240cd6`](96240cd6-48f2-46df-9ba0-458b8dfcae91), verified against raw logs/lock/source directly)
**Purpose:** Ground-truth status of the stalled production-overnight pipeline + a prioritized roadmap + a concrete plan for today.

---

## 1. Production overnight postmortem (Jun 26 → Jun 29)

### 1.1 Timeline

| Run | Started | Ended | Outcome |
|---|---|---|---|
| `production_overnight_20260626_163221.log` | 2026-06-26 16:32 | 2026-06-26 17:20 | D17 ✓, D21 ✓, cell sweep **aborted immediately on variant B0** — `$ErrorActionPreference=Stop` treated stderr progress (`[cell_sweep] variant B0 (1/25)`) as fatal |
| `production_overnight_20260628_170231.log` | 2026-06-28 17:02 | 2026-06-28 17:06 | Died during D17 startup (separate short-lived attempt) |
| `production_overnight_20260628_170701.log` | 2026-06-28 17:07 | **2026-06-29 04:41** | D17 ✓ (BPC 3.073, κ 0.860, math-integration), D21 ran but **produced no captured stdout** (anomaly, see 1.4), cell sweep progressed through **18 of 25 variants** then **crashed with a native access violation** |

The stderr fix (`Invoke-NativeWithProgressLog` in `scripts/run_d17_overnight.ps1:57-71`, uncommitted) **did work** — the sweep no longer aborts on the first progress line and got 18 variants further than the June 26 run. One cosmetic issue remains: the very first stderr line of a sweep (`variant B0 (1/25)`) is still rendered as a formatted `NativeCommandError`/`RemoteException` block in the transcript (`production_overnight_20260628_170701.log:625-630`), even though `$ErrorActionPreference = "Continue"` prevents it from being fatal. All subsequent variant lines print as clean text. Low priority — cosmetic only.

### 1.2 The real blocker: native crash in cell sweep variant H15

```
[cell_sweep] variant H14 (17/25)
[cell_sweep] variant H15 (18/25)
PS>TerminatingError(): "overnight run failed exit=-1073741819"
```
— `bench/results/production_overnight_20260628_170701.log:647-649`

`-1073741819` = `0xC0000005` = **Windows ACCESS_VIOLATION**. This is a native crash inside `cypha_cell_hypothesis_sweep.exe`, not a scripting bug. It happened **after 300k-scale training started for variant H15** ("AXIOM-evolved cell") — the previous 22 fast/medium-tier smoke runs (n_train ∈ {80, 200}) never hit it, so the fault is scale-dependent (300k steps) or state-dependent (something left over from H01–H14 that only corrupts memory far enough into H15's run).

**H15 config** (`native/src/cyphalm/cypha_cell_hypothesis.cpp:136-138`):
```cpp
} else if (id == "H15") {
    cfg.use_axiom_activation = true;
    cfg.alpha_learnable = true;
```

**Gate evaluation path** (`native/src/cyphalm/char_lstm.cpp:121-135`, `native/include/cypha/cyphalm/axiom_activation.hpp:21-31`): per-dimension enum-dispatch to `sigmoid`/`tanh`/`eml_nand`, size-guarded (`axiom_grammar_.i_gate.size() == hidden`). Nothing in this path is obviously unsafe (no recursion, no unchecked indices, fixed-size vectors set once at construction) — which suggests the corruption most likely originates **elsewhere** (e.g. state carried over from a prior variant's destructor/reset, a shared buffer sized for a different `hidden`/`n_experts` combination, or heap corruption from an earlier variant that only crashes when H15 touches the corrupted region). **This needs a targeted repro, not a guess** — see the plan below.

### 1.3 `bench/BASELINE_LOCK.json` is still stale

Because the cell sweep never finished, `run_overnight_all.ps1` never reached its lock-merge stage (`update_baseline_lock.ps1 -Run d17/d21/cell-sweep`), so nothing in the lock was refreshed:

| Section | `run_at` | Age | Status |
|---|---|---|---|
| `overnight_results` | 2026-06-17T09:46:05Z | 24 days | production (June 17 data, D17 BPC 2.864) |
| `rpsm_results` | 2026-06-17T09:49:04Z | 24 days | production (D21 BPC 7.336) |
| `cell_sweep_results` | 2026-06-17T20:59:40Z | 24 days | production (25 variants, best BPC 2.864) |
| `math_integration_results` | 2026-06-24T07:55:26Z | 17 days | **medium_smoke only**, n_train=5000 — 300k production tier never filled |

The June 28 D17 run *did* produce a fresh math-integration result (BPC 3.073, κ 0.860 @ 300k, captured in the log) but it was **never merged** because the pipeline crashed downstream before the merge step runs.

### 1.4 Anomaly to double-check: D21 RPSM produced no visible stdout

In `production_overnight_20260628_170701.log:618-621`:
```
== D21 RPSM overnight bench (profile=d21 mode=rpsm n_train=300000) ==

Done. Results under bench/results or repo results/.
```
No BPC JSON appears between the header and "Done.", unlike the June 26 log where D21 printed a full JSON block (`bpc: 7.336...`). `run_rpsm_overnight.ps1` calls the bench binary directly (no piping), so its stdout should land in the `Start-Transcript` capture the same way D17's did. Possible cause: `$env:CYPHA_OVERNIGHT_MATH_INTEGRATION` was left set by the preceding D17 `-MathIntegration` step (env vars persist across `&`-invoked child scripts in the same PowerShell session) and `--mode rpsm` may not handle that env var the same way `--mode hybrid` does. **Needs verification** — rerun D21 standalone and confirm output.

### 1.5 Git state

- `scripts/run_d17_overnight.ps1` — stderr fix, **uncommitted**.
- `bench/BASELINE_REPORT.md` — timestamp-only diff from a partial finalize retry, **uncommitted**.
- Last lock commit: `4d9a567` "bench: lock production overnight results (n_train=300000)" — **2026-06-18**, i.e. 23 days stale.
- `commit_production_lock.ps1 -Force` has not run since.

---

## 2. Roadmap research — current state of the art

### 2.1 Measured numbers (locked / logged)

| Metric | Value | Source |
|---|---|---|
| D17 hybrid BPC @ 300k (pin) | **2.873** | `bench/BASELINE_LOCK.json:39-50` |
| D17 hybrid BPC @ 300k (production overnight, Jun 17) | **2.864** | `bench/BASELINE_LOCK.json:70-88` |
| Cell sweep best BPC @ 300k (25 variants) | **2.864** | `bench/BASELINE_LOCK.json:2-18` |
| Math-integration BPC @ 5k (baseline → math) | 4.001 → **3.983** (ΔBPC −0.017) | `bench/BASELINE_LOCK.json:53-68` |
| Math-integration κ @ 5k (baseline → math) | 0.826 → **0.870** (Δκ +0.044) | same |
| Math-integration BPC/κ @ 300k, uncommitted (Jun 28) | BPC **3.073**, κ **0.860** | `bench/results/production_overnight_20260628_170701.log:25,203` |
| RPSM @ 300k | BPC **7.336** (far behind hybrid) | `bench/BASELINE_LOCK.json:90-107` |

### 2.2 Paper targets (not yet measured at scale)

| System | κ (target/estimate) | Source |
|---|---|---|
| Current hybrid (partial profile) | ~0.77 | `docs/research/intelligence_stats/cypha_self_correcting_paper4.md:662` |
| Full self-correcting Cypha, 256-dim (Paper IV) | 0.89 | `:654` |
| **At 1024 hidden dim** | **0.91–0.92** (human-median parity) — "no new theoretical breakthroughs" needed | `:671-695`, `:777` |
| Soft-world mature model (Paper V) | 0.90 @ 256-dim, **0.93 @ 1024-dim** | `docs/research/intelligence_stats/soft_world_paper5.md:585,658,715` |

**Diagnosis:** the remaining κ gap to human-median is attributed to representational scale (`D_eff`), not the epistemic axes — i.e. it's a scale-up problem, not a new-architecture problem (`cypha_self_correcting_paper4.md:671-695`).

### 2.3 Phase status

- **Last shipped:** Phase 39 (v2.3.39, 2026-06-23) — math-integration preset ship-lock.
- **Phases 40–59:** prep-complete at 5k ablation scale; **300k production certification not filled**.
- **Explicit "Still planned"** (`docs/reports/INTELLIGENCE_STATS_IMPLEMENTATION.md:813-818`):
  1. Phase 60 production math certificate (d53–d58 → `production_*_ready`)
  2. D17 300k + 25-variant production overnight lock fill — **this is exactly what's stuck, see §1**
  3. GitHub release publish (blocked on `gh auth login`)
  4. RPSM @ 300k production benchmark in CI

### 2.4 Other planned/future-work items found across `docs/`

Full list with citations is in the research transcript ([`96240cd6`](96240cd6-48f2-46df-9ba0-458b8dfcae91)); highlights:

- Paper IV Phase 5 hidden-dim scale experiment (512→1024) — theoretical κ ≥ 0.91 unlock
- Paper V causal graph beyond stub (`native/include/cypha/intelligence/causal_graph.hpp:13`)
- RPSM Option A (CyphaDIF matrix refactor) → Option B (sequence layer) — `docs/RESEARCH_STATUS.md:389-395`
- Kernel LLR tuning — ~18pp sklearn XOR gap remains — `docs/RESEARCH_STATUS.md:399-411`
- Multi-view online training Phase 2 (CyphaDIF) — `docs/MULTI_VIEW_TRAINING_PLAN.md:257-268`
- Known stub functions (not urgent, just noted): `causal_graph.hpp:13`, `reversible_ssm_cell.hpp:7-16` (`backward_stub`), `cyphalm_model.hpp:248` (`rpsm_embed_backprop_stub`), `ewc_regularizer.hpp:11` (diagonal Fisher stub)

---

## 3. Prioritized next-development-phase candidates

| # | Item | Why | Blocked by |
|---|---|---|---|
| **P0** | Fix cell-sweep crash resilience + finish production math certification (Phase 60) | Directly blocks lock refresh; all 24 math-ablation grids are already joint-ready at 5k | H15 native crash (§1.2) |
| **P1** | Hidden-dim scale experiment (512 → 1024) | Paper IV: κ ≥ 0.91 achievable with no new theory, purely a scale-up | P0 lock refresh (want a clean baseline first) |
| **P2** | D17 300k math-integration + cell-sweep Pareto lock refresh | Hybrid already 2.864 BPC; math preset should add κ without BPC regression | P0 |
| **P3** | Paper V soft-world / causal graph maturity | Next framework beyond 7-stat profile; targets κ 0.90–0.93 | independent, longer horizon |
| **P4** | RPSM Option A → Option B | RPSM @ 7.336 BPC vs hybrid 2.864 — biggest single BPC gap in the repo | independent |
| **P5** | Kernel LLR tuning (close 18pp XOR gap) | Confirmed hard ceiling, partially shipped | independent |
| **P6** | Multi-view training Phase 2 (CyphaDIF) | LM side done; DIF side not started | independent |
| **P7** | Release publish + `gh auth login` | Blocked purely on maintainer auth, not code | none (just needs `gh auth login`) |

**Critical path for κ:** fix crash → certify math preset @ 300k (P0) → scale hidden dimension (P1) → soft-world layer (P3).

---

## 4. Plan for today (2026-07-11)

Ordered so each step unblocks the next; stop and reassess after any red flag.

1. **Commit the stderr fix** already sitting uncommitted in `scripts/run_d17_overnight.ps1` (it demonstrably helped — got 18/25 variants further than without it). Low risk, no reason to keep it uncommitted.
2. **Reproduce the H15 crash in isolation** — run `cypha_cell_hypothesis_sweep --overnight-sweep --profile d17 --n-train 300000` filtered to just variants H14→H16 (or however the tool supports a subset) to confirm it's H15-specific and scale-triggered, not order-dependent state corruption from H01-H14. If a `--variant` / `--only` flag doesn't exist on the sweep tool, add one — it's useful for exactly this kind of bisection going forward.
3. **Root-cause the access violation** — likely candidates to check first, in order of suspicion:
   - Memory/state not reset between variants in `cypha_cell_hypothesis_sweep.cpp` (look for reused buffers sized for a previous variant's `hidden`/`n_experts`/`n_train` config)
   - `AddressSanitizer` or `/fsanitize=address` build of `cypha_cell_hypothesis_sweep` + a small-n_train repro of H15 alone, to catch heap corruption before it manifests as a crash 18 variants later
   - Confirm the `axiom_grammar_` size guard (`char_lstm.cpp:123`) never silently falls through when `hidden` mismatches (guard exists but check what happens on the `else` — does it fall to EML/sigmoid silently, and would *that* explain divergent numerics rather than the crash itself?)
4. **Harden the sweep runner** regardless of root cause: isolate each variant in its own subprocess (or at least wrap in a structured-exception/signal handler on Windows) so **one variant's crash doesn't discard the other 24 variants' worth of already-completed work**. This is the single highest-leverage fix — it turns "lose an entire overnight run to one bad variant" into "lose 45 minutes and get 24/25 results anyway."
5. **Re-run the production overnight** (`scripts/run_production_overnight.ps1 -BuildDir native/build_math -MathIntegration`) once 1 and 4 are in, in a **Cursor-managed terminal** (not an external `Start-Process` window — this has caused visibility confusion in prior sessions).
6. **Verify the D21 stdout anomaly** (§1.4) independently — run `scripts/run_rpsm_overnight.ps1 -Production` alone and confirm BPC JSON prints before trusting the next full pipeline run.
7. **On completion:** run `finalize_production_overnight.ps1` → `commit_production_lock.ps1 -Force` to land the refreshed lock (D17 math-integration BPC/κ @ 300k, D21, and a real 28-variant — or however many survive — cell sweep).
8. **If time remains:** start scoping P1 (hidden-dim scale experiment) — read `cypha_self_correcting_paper4.md:671-769` in full for the exact Phase 1-5 implementation roadmap it lays out, and check what `--hidden-dim` / config plumbing already exists vs. needs adding.

### Commands reference

```powershell
# 1. Commit the fix (after reviewing diff)
git add scripts/run_d17_overnight.ps1
git commit -m "fix: prevent native stderr progress lines from aborting overnight scripts"

# 2/3. Isolate H15 repro (adjust once sweep tool subset flag confirmed/added)
native/build_math/cypha_cell_hypothesis_sweep.exe --overnight-sweep-smoke --profile d17 --n-train 5000 --n-eval 256 --threads 1

# 5. Re-run full pipeline in a Cursor terminal
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/run_production_overnight.ps1 -BuildDir native/build_math -MathIntegration

# 6. Verify D21 alone
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/run_rpsm_overnight.ps1 -Production

# 7. Finalize + commit lock
powershell -File scripts/finalize_production_overnight.ps1 -BuildDir native/build_math
powershell -File scripts/commit_production_lock.ps1 -BuildDir native/build_math -Force

# Monitor throughout
powershell -File scripts/watch_production_overnight.ps1
```

---

## 5. Open questions for the user

- Should step 4 (subprocess-isolate each cell-sweep variant) happen before or after re-running the full pipeline once more as-is? Running once more without the fix would at least tell us if H15 crashes deterministically every time or is intermittent.
- Priority after P0/P2 land: P1 (hidden-dim scale, theory-backed κ win) vs P4 (RPSM, biggest raw BPC gap)? Both are independent and could be parallelized across sessions.
