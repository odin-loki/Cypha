# Two-stage pipeline parity

**`two_stage_dif_predict_with_clf`** vs sidecar goldens.

**Frozen fixture.** Gate: **`ctest -R native_regression_two_stage_pipeline`**. Override: **`CYPHA_TWO_STAGE_PIPELINE_PARITY_BIN`**.
