# Two-stage e2e ridge parity

End-to-end ridge stages on **frozen router LLR** from sidecar (quantile router training goldens committed).

**Frozen fixture.** Gate: **`ctest -R native_regression_two_stage_e2e_ridge`**.
