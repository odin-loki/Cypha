# d38 overnight certificate — 2026-07-18

**Verdict:** **PASS — `overnight_certificate_ready`**

Validated against lock commit **`a552aee`** (`bench/BASELINE_LOCK.json` → smoke fixture at configure time).

| Field | Value |
|-------|-------|
| `validation_status` | **overnight_certificate_ready** |
| `production_status` | production_validated |
| `overnight_complete_status` | overnight_complete_validated |
| `n_train` | 300000 |
| `variant_count` | **25** (≥ `kExpectedCellSweepVariants`; sufficient) |
| Certificate scripts | `run_post_overnight.ps1` + `validate_production_complete.ps1` present |

## Tree references

| Item | Location |
|------|----------|
| CTest | `native_d38_overnight_certificate_smoke` — `native/CMakeLists.txt` |
| Domain runner | `run_d38_overnight_certificate_validation()` — `native/src/bench/bench_domains.cpp` |
| Profile | `bench/config/d38_overnight_certificate_profile.json` |
| Validate hook | `CYPHA_VALIDATE_OVERNIGHT_CERTIFICATE=1` on `scripts/cypha_native_validate_all.ps1` |

## Commands (2026-07-18 run)

```powershell
# One-time configure (if native/build_d38 missing)
cmake -S native -B native/build_d38 -DCMAKE_BUILD_TYPE=Release -G Ninja
cmake --build native/build_d38 --target cypha_bench_run --parallel

# Blocking gate smoke (recommended)
ctest --test-dir native/build_d38 -R native_d38_overnight_certificate_smoke --output-on-failure
# Result: Passed (0.66 s), exit 0

# Equivalent direct run (cwd must be build dir for lock fixture)
Push-Location native/build_d38
$env:CYPHA_BENCH_FAST = "1"
.\cypha_bench_run.exe --domain-tag d38
Pop-Location

# Extended validate-all hook (optional)
$env:CYPHA_VALIDATE_OVERNIGHT_CERTIFICATE = "1"
powershell -File scripts/cypha_native_validate_all.ps1
```

**Report:** `bench/report/tables/d38_overnight_certificate_validation.json`

**Note:** d38 may rewrite `bench/BASELINE_REPORT.md` — restore with `git checkout -- bench/BASELINE_REPORT.md` if unclean.

**Stale builds:** `C:\Temp\cypha_full_cpp_build` lacks `--domain-tag` / d38 CTest; use `native/build_d38` or reconfigure.
