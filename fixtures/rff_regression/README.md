# RFF regression parity

RFF encode + ridge + expert dot kernels vs native **`regression_rff_parity`**.

**Frozen fixture.** Gate: **`ctest -R native_regression_rff`**. Override: **`CYPHA_REGRESSION_RFF_PARITY_BIN`**.
